#include<stdio.h>
#include<math.h>
int main(){
   int num;
   scanf("%d",&num);
   for(int i=1; i<=num;i+=1){  // here we can also use i<=num/2 
       if(num%i==0)
       printf("%d ",i);
   }
    return 0;
}

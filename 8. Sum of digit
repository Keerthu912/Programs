#include <stdio.h>
int main()
{
    int digit;
    int sum = 0;
    scanf("%d",&digit);
    while(digit>0)
    {
       int temp = digit%10;
       sum += temp;
       digit /= 10;
    }
    printf("sum=%d",sum);
}

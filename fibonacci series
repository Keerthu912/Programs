#include <stdio.h>

int main()
{
  int prev,curr,nxt,i,n;
  prev=curr=1;
  printf("Enter a term:");
  scanf("%d",&n);
  printf("%d %d ",prev,curr);
  i=1;
  while(i<=n-2)
  {
    nxt=curr+prev;
    printf("%d ",nxt);
    prev=curr;
    curr=nxt;
    i=i+1;
  }
}

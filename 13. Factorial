#include<stdio.h>
int main()
{
    int input;
    scanf("%d",&input);
    int fact = 1;
    for(int i=input; i>0; i-=1)
    {
        fact = fact*i;
    }
    printf("factorial of %d is %d",input,fact);
}
